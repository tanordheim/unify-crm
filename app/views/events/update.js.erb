$.closeModal();

<% if event.errors.any? -%>
  $.modal("Modify event", "<%= escape_javascript(render('form')) %>", function() {
    $.updateAddLinkState();
  });
<% else -%>
  $('#calendar').fullCalendar('refetchEvents');
<% end -%>
