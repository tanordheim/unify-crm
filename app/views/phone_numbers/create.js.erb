<% unless phone_number.errors.any? -%>
  $.getScript('/<%= phoneable_type.to_s.pluralize %>/<%= phoneable_id %>/phone_numbers', function() {
    $('li[data-phone-number-id="<%= phone_number.id.to_s %>"]').effect('highlight', {}, 'fast');
  });
<% end %>
