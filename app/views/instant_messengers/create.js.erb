<% unless instant_messenger.errors.any? -%>
  $.getScript('/<%= instant_messageable_type.to_s.pluralize %>/<%= instant_messageable_id %>/instant_messengers', function() {
    $('li[data-instant-messenger-id="<%= instant_messenger.id.to_s %>"]').effect('highlight', {}, 'fast');
  });
<% end %>
